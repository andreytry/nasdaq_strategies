//@version=5
strategy("MACD and RSI Strategy with Custom EMAs, Trailing Stop-Loss, Price Move Alerts, and MACD Notifications", overlay=true)

// MACD calculations
[macdLine, signalLine, _] = ta.macd(close, 12, 26, 9)

// RSI calculation
rsi = ta.rsi(close, 14)

// EMA calculations with custom RGB colors
ema40 = ta.ema(close, 40)
ema20 = ta.ema(close, 20)
ema200 = ta.ema(close, 200)

// Define custom colors with RGB values
colorCyan = color.new(#00FFFF, 0)  // Cyan color for EMA40
colorPink = color.new(#FFC0CB, 0)  // Pink color for EMA20
colorYellow = color.new(#FFFF00, 0) // Yellow color for EMA200

// Plot the EMAs with custom colors
plot(ema40, color=colorCyan, linewidth=2, title="EMA 40")
plot(ema20, color=colorPink, linewidth=2, title="EMA 20")
plot(ema200, color=colorYellow, linewidth=2, title="EMA 200")

// Trailing Stop-Loss Constants
var float initialOffset = 160.0  // Initial offset for stop-loss (160 points)
var float trailingOffset = 320.0  // Trailing offset for stop-loss (320 points)

// Define the threshold for price change alerts
var float priceChangeThreshold = 10.0  // Threshold for triggering price change alerts

// Track RSI oversold and overbought conditions
rsiOversold = rsi < 30
rsiOverbought = rsi > 70

// Track RSI oversold and overbought within the last 10 candles
rsiOversoldRecent = ta.lowest(rsi, 10) < 35
rsiOverboughtRecent = ta.highest(rsi, 10) > 65

// Check if RSI "low" or "high" labels have already been placed in the last 10 candles
var int lastRSILowLabel = na
var int lastRSIHighLabel = na

// MACD crossover conditions
macdCrossOver = ta.crossover(macdLine, signalLine)
macdCrossUnder = ta.crossunder(macdLine, signalLine)

// Plot green lower triangle for MACD Crossover (bullish)
plotshape(series=macdCrossOver, location=location.belowbar, color=color.green, style=shape.triangleup, size=size.small, title="MACD Crossover")

// Plot red upper triangle for MACD Crossunder (bearish)
plotshape(series=macdCrossUnder, location=location.abovebar, color=color.red, style=shape.triangledown, size=size.small, title="MACD Crossunder")

// Detect changes in MACD slope
macdSlopeChangePositive = (macdLine[1] < macdLine[2]) and (macdLine > macdLine[1])  // MACD slope turns positive
macdSlopeChangeNegative = (macdLine[1] > macdLine[2]) and (macdLine < macdLine[1])  // MACD slope turns negative

// Detect EMA200 trend
ema200TrendingUp = ema200 > ema200[1]  // EMA200 trending up
ema200TrendingDown = ema200 < ema200[1]  // EMA200 trending down

// Buy condition: MACD slope turns positive and RSI was oversold within the last 10 candles (no open short position)
longCondition = macdSlopeChangePositive and rsiOversoldRecent and strategy.position_size <= 0
if (longCondition)
    strategy.entry("Buy", strategy.long)
    label.new(bar_index, low - 20, text="B", color=color.green, textcolor=color.white, size=size.normal)  // Positioned 20 points below the low

// Sell condition: MACD slope turns negative and RSI was overbought within the last 10 candles (no open long position)
shortCondition = macdSlopeChangeNegative and rsiOverboughtRecent and strategy.position_size >= 0
if (shortCondition)
    strategy.entry("Sell", strategy.short)
    label.new(bar_index, high + 20, text="S", color=color.red, textcolor=color.white, size=size.normal)  // Positioned 20 points above the high

// Implement Trailing Stop-Loss for Long and Short Positions
strategy.exit("Exit Buy", "Buy", trail_offset=trailingOffset, trail_points=initialOffset)
strategy.exit("Exit Sell", "Sell", trail_offset=trailingOffset, trail_points=initialOffset)

// Remove RSI alerts and print only labels for oversold/overbought conditions (no RSI alerts)
if (rsiOversold and (na(lastRSILowLabel) or (bar_index - lastRSILowLabel > 10)))
    label.new(bar_index, low - 50, text="RSI", color=color.blue, textcolor=color.white, size=size.normal)  // Positioned 50 points below
    lastRSILowLabel := bar_index

if (rsiOverbought and (na(lastRSIHighLabel) or (bar_index - lastRSIHighLabel > 10)))
    label.new(bar_index, high + 50, text="RSI", color=color.orange, textcolor=color.white, size=size.normal)  // Positioned 50 points above
    lastRSIHighLabel := bar_index

// Calculate price change over 30 bars (assuming 1-second chart for 30 seconds)
bars_ago = 30
price_change = close - close[bars_ago]

// Trigger alert if price moves up or down by more than the threshold (10 points)
if (math.abs(price_change) > priceChangeThreshold)
    label.new(bar_index, na, text="!!!", color=color.yellow, textcolor=color.black, size=size.normal)  // Label with 3 exclamation marks
    alert("Price moved by more than " + str.tostring(priceChangeThreshold) + " points in 30 seconds", alert.freq_once_per_bar)

// Notify on MACD up when EMA200 is trending up
if (macdSlopeChangePositive and ema200TrendingUp)
    alert("MACD UP with EMA200 trending up", alert.freq_once_per_bar)

// Notify on MACD down when EMA200 is trending down
if (macdSlopeChangeNegative and ema200TrendingDown)
    alert("MACD DOWN with EMA200 trending down", alert.freq_once_per_bar)
